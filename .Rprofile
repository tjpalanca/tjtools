# Activate renv environment
source("renv/activate.R")

# User level Rprofile
if (file.exists("~/.Rprofile")) source("~/.Rprofile", chdir = TRUE)

# Development Utilities
if (interactive()) source("dev/utils.R")
